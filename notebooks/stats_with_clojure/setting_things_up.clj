;; # Setting Things Up

^:kindly/hide-code
(ns stats-with-clojure.setting-things-up 
  (:require [src.helpers :as h]))

(h/md "setting_things_up.md")

;; ## Running this book

;; Have REPl running

;; custom REPL commands

(h/img "custom_repl_command_in_vscodium.png")

;; quarto

(h/img "create_html_using_quarto.png")

;; code and HTML side by side

(h/img "executing_notebook_with_quarto.png")

;; REPL

(h/img "executing_in_repl.png")
